<%#dashboard.ejs kullanıcının kendi "mutfağı" %>

<div class="row">
    <div class="col-md-3 border-end pe-3">
        <%- include('partials/_sidebar') %>
        
    </div>

    <div class="col-md-9">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1>Yazılarım</h1>
            
        </div>
        <hr>

        <% if (news && news.length > 0) { %>
            <ul class="list-group list-group-flush">
                <% news.forEach(news => { %>
                    <li class="list-group-item d-flex justify-content-between align-items-center">
                        <div>
                            <a href="/news/<%= news._id %>" class="fw-semibold"><%= news.title %></a> <br>
                            <small class="text-muted">
                                Yayınlandı: <%= new Date(news.createdAt).toLocaleDateString('tr-TR') %>
                                <% if (news.category) { %> - Kategori: <%= news.category %> <% } %>
                            </small>
                        </div>
                        <div>
                            <a href="/news/<%= news._id %>/edit" class="btn btn-sm btn-outline-secondary me-1">Düzenle</a>
                            <form action="/news/<%= news._id %>/delete" method="news" class="d-inline" onsubmit="return confirm('Bu yazıyı silmek istediğinizden emin misiniz?');">
                                <button type="submit" class="btn btn-sm btn-outline-danger">Sil</button>
                            </form>
                        </div>
                    </li>
                <% }) %>
            </ul>
        <% } else { %>
            <div class="alert alert-light text-center" role="alert">
                Henüz hiç yazı oluşturmadınız.
            </div>
        <% } %>
    </div>
</div>